/**
 * Resources-first pivot: the course platform (courses, Pro subscriptions,
 * progress tracking) is hidden until the course launches. Flip this to true
 * to bring back the course/Pro UI on the settings page. Nav links, footer
 * links, and the landing page were edited directly — see the redirects in
 * next.config.ts and src/app/_archive/ for those.
 */
export const COURSES_ENABLED = false;
